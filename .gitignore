
dist/
dist
.env
.ledo-mode
# Root build artifacts — anchored so nested paths named "main"/"ledo"
# (e.g. .apm/skills/ledo/) are NOT ignored.
/main
/ledo
.ledo-back.yml
.scannerwork
result
vendor
.direnv
.serena
